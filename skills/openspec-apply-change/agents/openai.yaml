interface:
  display_name: "Openspec Apply Change"
  short_description: "Use the shared OpenSpec apply change workflow"
  default_prompt: "Use $openspec-apply-change with this repository's pinned CLI and authorized scope."
policy:
  allow_implicit_invocation: true
