interface:
  display_name: "Openspec Propose"
  short_description: "Use the shared OpenSpec propose workflow"
  default_prompt: "Use $openspec-propose with this repository's pinned CLI and authorized scope."
policy:
  allow_implicit_invocation: true
