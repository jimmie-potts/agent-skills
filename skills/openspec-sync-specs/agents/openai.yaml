interface:
  display_name: "Openspec Sync Specs"
  short_description: "Use the shared OpenSpec sync specs workflow"
  default_prompt: "Use $openspec-sync-specs with this repository's pinned CLI and authorized scope."
policy:
  allow_implicit_invocation: true
