interface:
  display_name: "Openspec Update Change"
  short_description: "Use the shared OpenSpec update change workflow"
  default_prompt: "Use $openspec-update-change with this repository's pinned CLI and authorized scope."
policy:
  allow_implicit_invocation: true
