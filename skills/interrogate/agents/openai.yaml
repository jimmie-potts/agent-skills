interface:
  display_name: "Interrogate"
  short_description: "Run independent adversarial code reviews"
  default_prompt: "Use $interrogate to challenge this code change with independent reviewers and deliver a verified lead verdict."

policy:
  allow_implicit_invocation: false
