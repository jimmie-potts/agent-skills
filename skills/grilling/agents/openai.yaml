interface:
  display_name: "Grilling"
  short_description: "Stress-test decisions in dependency-aware rounds"
  default_prompt: "Use $grilling to map this decision tree, ask the current independent frontier as one numbered group, and stop before action."

policy:
  allow_implicit_invocation: true
