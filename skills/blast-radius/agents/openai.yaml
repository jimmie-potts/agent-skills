interface:
  display_name: "Blast Radius"
  short_description: "Prove what a change could break beyond its diff"
  default_prompt: "Use $blast-radius to assess what this change could break and prove its load-bearing safety claim."

policy:
  allow_implicit_invocation: false
