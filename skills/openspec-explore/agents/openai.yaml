interface:
  display_name: "Openspec Explore"
  short_description: "Use the shared OpenSpec explore workflow"
  default_prompt: "Use $openspec-explore with this repository's pinned CLI and authorized scope."
policy:
  allow_implicit_invocation: true
