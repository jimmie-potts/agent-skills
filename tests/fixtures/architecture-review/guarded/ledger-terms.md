# Terms

Ledger API: the provider client, currently the only production implementation.
Ledger adapter: the stable internal contract used by applications. Provider
exceptions must not escape it. Amounts use integer cents.
