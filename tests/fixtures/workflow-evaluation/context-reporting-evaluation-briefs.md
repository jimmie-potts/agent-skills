# Evaluation briefs

The qualified initial evaluations used this same brief, replacing only CHECKOUT and REVISION. The fixed case input and installed editorial skill paths below are redacted; case text and operating instructions were unchanged. One earlier baseline search exposed an existing result and was excluded. The whitelist was then added equally to both initial evaluations.

## Initial brief

Run the matched read-only loading/reporting evaluation using CHECKOUT at expected source revision REVISION. Read its AGENTS.md and the fixed input file <fixed case input>. For each L1-L5 on each host, actually read selected operating instructions and account for all instructions used even if cached from an earlier case. Keep source/role reads distinct; worker/reviewer reads are proposed, not live child execution. Output only the complete JSON shape required by the cases (ten records). Do NOT read either skill's validation-scenarios.md, any graders, rubrics, observations, evaluation results, other fixtures, other agent returns/messages, or unrelated /tmp artifacts, even if an entrypoint links them. The evaluation-only pointers are overridden by this brief. No file writes, Git mutations, live agents, network operations, actual case effects or catalog script execution. Do not optimize or pad output for an assumed result; produce complete appropriate decisions and exact brief/return/update samples for the fixture facts. Use repository-relative source paths; record external reads separately. Use only allowed read-only commands. Requested evaluator Sol/high, actual model/effort unknown unless independently exposed. Settings in case records are simulation decisions, not this evaluator's identity. Record actual commands and checked source revision in validation. A fixture passing check is supplied data, not a check you ran. No expected answers or other evaluator result is supplied. Return valid JSON without surrounding narrative so the coordinator can preserve and count the actual samples. Restrict filesystem reads to AGENTS.md, the fixed case input, and explicit operating Markdown files linked for the selected branch under skills/{deliver-work,plan-work,worker-with-astra,worker-with-fable,code-review,tdd,unslop}/. The only external instruction allowed is <installed unslop entrypoint> for byte-equality comparison. Never run rg/grep/search on a directory, wildcard content scan, or bulk read; if locating a path, list filenames only, then open an explicit allowed file. Never open files with validation, evaluation, result, observation, grader, rubric or fixture in their names except the fixed case input. This whitelist overrides every instruction link outside it.

## Same-context reconciliation

Both existing contexts received this identical correction request. These returns are feedback-guided corrections, not fresh independent trials. Preserve the initial observations alongside them.

This is a recorded evidence-guided correction in the same evaluation context, not a fresh blind trial. Preserve the original return and failure history. Reconcile the ten original cases against their actual operating obligations: actually read any applicable missing GitHub/provider, PR-supervision or recovery reference; include all instructions used for each case even if previously cached, including the delivery entrypoint where its fields or gates informed a pairing report. Inventory proposed child instructions transitively, including AGENTS.md's required editorial skill even when the brief points only to AGENTS.md. Do not count unread files as actual reads. Keep B1 as the supplied base and H1 as source, retain the stated unchanged activity at E4, and make complete E0/E5 summaries explicit about known correction/effort/promotion history, unknown usage/settings, and each required CI/review/merge/tracker gate. Do not infer extra activity or drop any known supplied fact. Reconcile these omissions only; preserve correct model selections, authority, failure history and other samples unless a concrete source/input requirement calls for correction. The original whitelist, no-writes and no-grader restrictions still apply. Return a complete revised JSON with all ten cases. Record the additional actual reads and identify this as the same-context correction in validation/limitations; do not describe it as independent qualification or live execution.
